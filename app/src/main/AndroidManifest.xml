<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vipvideo">

    <application
        android:name=".app.UApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DayTheme"
        tools:replace="android:icon">

        <activity
            android:name=".ui.WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TabsActivity" />
        <activity
            android:name=".ui.video.AllVideoActivity"
            android:label="电影筛选" />
        <activity
            android:name=".ui.video.VideoPlayerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:label="电影播放" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="登录" />
        <activity
            android:name=".ui.login.RegisterActivity"
            android:label="注册" />
        <activity
            android:name=".ui.login.ForgetPasswordActivity"
            android:label="找回密码" />


        <activity android:name=".ui.reader.ReadActivityNew" />
    </application>

</manifest>
